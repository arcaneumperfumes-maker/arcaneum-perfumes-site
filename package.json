{
  "name": "arcaneum-perfumes-site",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "copycat:baseline": "node scripts/build-copycat-baseline.mjs",
    "copycat:check": "node scripts/build-copycat-baseline.mjs --check && node scripts/scan-copycat-watch.mjs",
    "test": "node --test"
  },
  "devDependencies": {
    "vite": "latest"
  }
}
